{
  "name": "website-project",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "create-dirs": "mkdir -p dist/css dist/js dist/img dist/\"Background Images\" dist/\"Photos for Website\"",
    "minify-css": "cssnano-cli css/*.css --dir dist/css --no-map",
    "minify-js": "terser js/*.js --compress --mangle --output dist/js/app.min.js",
    "copy-html": "cp *.html dist/",
    "copy-assets": "cp -R img/ dist/img/ && cp -R \"Background Images\"/ dist/\"Background Images\"/ && cp -R \"Photos for Website\"/ dist/\"Photos for Website\"/",
    "build": "npm run clean && npm run create-dirs && npm run minify-css && npm run minify-js && npm run copy-html && npm run copy-assets"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "cssnano-cli": "^1.0.5",
    "terser": "^5.39.0"
  }
}
